import { supportsDialog } from "../utils/browserCompatibility.js";
export function mountDeleteConfirmModal(identifier, content) { document.body.insertAdjacentHTML("beforeend", `<dialog class="delete-modal" id="${identifier}"><form method="dialog"><h2>${content.title}</h2><div class="modal-actions"><button value="cancel">${content.cancel}</button><button class="confirm-delete" value="confirm"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 7h16M9 7V4h6v3m-8 0 1 13h8l1-13M10 11v5m4-5v5"/></svg>${content.confirm}</button></div></form></dialog>`); return document.getElementById(identifier); }
export function confirmDelete(modal) { if (!supportsDialog()) return Promise.resolve(window.confirm("打包扔掉？")); return new Promise(resolve => { modal.addEventListener("close", () => resolve(modal.returnValue === "confirm"), { once: true }); modal.showModal(); }); }
